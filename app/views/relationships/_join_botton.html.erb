<% unless current_user.joined_team?(team) %>
  <%= form_with(model: current_user.relationships.build, local: true) do |f| %>
    <%= hidden_field_tag :team_id, team.id %>
    <%= f.submit 'Join team', class: 'btn btn-primary btn-sm' %>
  <% end %>
<% end %>
  
